const feathers = require('@feathersjs/feathers');
const checkIfLinkExists = require('../../src/hooks/check-if-link-exists');

describe('\'checkIfLinkExists\' hook', () => {
  let app;

  beforeEach(() => {
    app = feathers();

    app.use('/dummy', {
      async get(id) {
        return { id };
      }
    });

    app.service('dummy').hooks({
      before: checkIfLinkExists()
    });
  });

  it('runs the hook', async () => {
    expect.assertions(1);
    const result = await app.service('dummy').get('test');
    expect(result).toEqual({ id: 'test' });
  });
});
